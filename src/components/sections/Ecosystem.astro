---
import { ShoppingBag, Shield, Factory, RefreshCw, Smartphone, Check } from '@lucide/astro';

const nodes = [
  {
    id: 'brands',
    icon: ShoppingBag,
    name: 'BRANDS',
    description: 'Manage product lines, oversee compliance, generate QR codes.',
    subdomain: 'app.passr.eu',
    tag: 'PAYING CLIENT',
    pos: { top: '0px', left: '50%', transform: 'translateX(-50%)' },
    pathId: 'path-brands',
    d: 'M 480,260 L 480,164',
    dots: [],
    begin: '0s'
  },
  {
    id: 'regulators',
    icon: Shield,
    name: 'REGULATORS',
    description: 'Automated machine-to-machine validation at EU borders.',
    subdomain: 'verify.passr.eu',
    tag: 'ENFORCER',
    pos: { top: '50%', left: '0px', transform: 'translateY(-50%)' },
    apiBadge: true,
    pathId: 'path-regulators',
    d: 'M 380,310 L 290,310 L 290,340 L 200,340',
    dots: [{x: 290, y: 310}, {x: 290, y: 340}],
    begin: '0.4s'
  },
  {
    id: 'suppliers',
    icon: Factory,
    name: 'SUPPLIERS',
    description: 'Upload lab reports, certify materials, attach data to SKUs.',
    subdomain: 'supplier.passr.eu',
    tag: 'DATA SOURCE',
    pos: { top: '50%', right: '0px', transform: 'translateY(-50%)' },
    pathId: 'path-suppliers',
    d: 'M 580,310 L 670,310 L 670,340 L 760,340',
    dots: [{x: 670, y: 310}, {x: 670, y: 340}],
    begin: '0.8s'
  },
  {
    id: 'recyclers',
    icon: RefreshCw,
    name: 'RECYCLERS',
    description: 'Access industrial teardown specs, hazardous material data.',
    subdomain: 'recycler.passr.eu',
    tag: 'END OF LIFE',
    pos: { bottom: '0px', left: '60px' },
    pathId: 'path-recyclers',
    d: 'M 440,360 L 440,480 L 280,480 L 280,540',
    dots: [{x: 440, y: 480}, {x: 280, y: 480}],
    begin: '1.2s'
  },
  {
    id: 'consumers',
    icon: Smartphone,
    name: 'CONSUMERS',
    description: 'Access product authenticity, materials, care instructions.',
    subdomain: 'verify.passr.eu',
    tag: 'END USER',
    pos: { bottom: '0px', right: '60px' },
    pathId: 'path-consumers',
    d: 'M 520,360 L 520,480 L 680,480 L 680,540',
    dots: [{x: 520, y: 480}, {x: 680, y: 480}],
    begin: '1.6s'
  }
];

const flowLabels = [
  { text: 'PASSPORT DATA', style: 'top: 120px; left: 50%; transform: translateX(-50%)' },
  { text: 'COMPLIANCE API', style: 'top: 300px; left: 170px' },
  { text: 'CERTIFICATIONS', style: 'top: 300px; right: 130px' },
  { text: 'TEARDOWN SPECS', style: 'bottom: 110px; left: 230px' },
  { text: 'QR SCAN', style: 'bottom: 110px; right: 200px' }
];

const stats = [
  { value: '5', label: 'STAKEHOLDER PORTALS' },
  { value: '2028', label: 'EU ENFORCEMENT DEADLINE', highlight: true },
  { value: 'GS1', label: 'DIGITAL LINK STANDARD' }
];
---

<section id="ecosystem" class="eco-section">
  <!-- Background Dotted Grid -->
  <div class="eco-grid-bg"></div>

  <div class="container-inner relative z-10">
    <!-- Section Header -->
    <header class="eco-header">
      <div class="eco-eyebrow">
        <span class="eyebrow-bar"></span>
        THE PASSR ECOSYSTEM
      </div>
      <h2 class="eco-headline">
        One infrastructure.<br />
        Five stakeholders.<br />
        <span class="text-signal">Zero gaps.</span>
      </h2>
      <p class="eco-subheadline">
        Passr connects every party in the product compliance chain —<br />
        from raw material supplier to end-of-life recycler.
      </p>
    </header>

    <!-- ── DESKTOP DIAGRAM ── -->
    <div class="diagram-viewport hidden lg:block">
      <div class="diagram-container">
        <!-- SVG Connections -->
        <svg class="connections-svg" width="960" height="620" viewBox="0 0 960 620" fill="none" xmlns="http://www.w3.org/2000/svg">
          {nodes.map(node => (
            <path id={node.pathId} d={node.d} class="connector-path" />
          ))}

          <!-- Static Connection Dots at Bends -->
          {nodes.flatMap(node => node.dots.map(dot => (
            <circle cx={dot.x} cy={dot.y} r="4" fill="#FF3B30" />
          )))}

          <!-- Animated Particles -->
          {nodes.map(node => (
            <g>
              <!-- Outbound -->
              <circle r="3" fill="#FF3B30" opacity="0.8">
                <animateMotion dur="2s" repeatCount="indefinite" calcMode="linear" begin={node.begin}>
                  <mpath href={`#${node.pathId}`} />
                </animateMotion>
              </circle>
              <!-- Inbound -->
              <circle r="3" fill="#FF3B30" opacity="0.4">
                <animateMotion dur="2s" repeatCount="indefinite" calcMode="linear" begin={node.begin} keyPoints="1;0" keyTimes="0;1">
                  <mpath href={`#${node.pathId}`} />
                </animateMotion>
              </circle>
            </g>
          ))}
        </svg>

        <!-- Center Hub -->
        <div class="center-hub">
          <div class="hub-wordmark">
            PASSR<span class="hub-dot">●</span>
          </div>
          <div class="hub-subtitle">THE CONNECTED DPP ECOSYSTEM</div>
          <div class="hub-metadata">EU ESPR · GS1 DIGITAL LINK</div>
        </div>

        <!-- Stakeholder Cards -->
        {nodes.map(node => (
          <div class="stakeholder-card" style={node.pos}>
            <div class="card-top">
              <node.icon size={32} stroke-width={1.5} class="card-icon" />
              <div class="check-badge">
                <Check size={12} stroke-width={3} class="text-signal" />
              </div>
            </div>
            <h3 class="card-title">{node.name}</h3>
            <p class="card-desc">{node.description}</p>
            <div class="card-bottom">
              <span class="card-subdomain">{node.subdomain}</span>
              <div class="flex items-center gap-2">
                {node.apiBadge && <span class="api-badge">API</span>}
                <span class="role-tag">{node.tag}</span>
              </div>
            </div>
          </div>
        ))}

        <!-- Data Flow Labels -->
        {flowLabels.map(label => (
          <div class="flow-label" style={label.style}>
            {label.text}
          </div>
        ))}
      </div>
    </div>

    <!-- ── MOBILE LAYOUT ── -->
    <div class="lg:hidden mobile-layout">
      <!-- Mobile Hub -->
      <div class="mobile-hub">
        <div class="hub-wordmark text-white">
          PASSR<span class="hub-dot">●</span>
        </div>
        <div class="hub-metadata text-[#858585] mt-2">
          EU ESPR COMPLIANT DPP INFRASTRUCTURE
        </div>
      </div>

      <!-- Vertical Connective Line -->
      <div class="mobile-connector-line"></div>

      <!-- Mobile Cards -->
      <div class="mobile-cards-container">
        {nodes.map(node => (
          <div class="mobile-card">
            <div class="card-top">
              <node.icon size={28} stroke-width={1.5} class="card-icon" />
              <div class="check-badge">
                <Check size={12} stroke-width={3} class="text-signal" />
              </div>
            </div>
            <h3 class="card-title">{node.name}</h3>
            <p class="card-desc">{node.description}</p>
            <div class="card-bottom">
              <span class="card-subdomain">{node.subdomain}</span>
              <span class="role-tag">{node.tag}</span>
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- ── STATS SECTION ── -->
    <div class="stat-grid">
      {stats.map(stat => (
        <div class="stat-item">
          <div class={`stat-value ${stat.highlight ? 'text-signal' : 'text-[#0A0A0A]'}`}>
            {stat.value}
          </div>
          <div class="stat-label">{stat.label}</div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .eco-section {
    background: #F8F8F8;
    padding: 120px 0;
    overflow: hidden;
    position: relative;
  }

  /* Dot Grid Background */
  .eco-grid-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background-image: radial-gradient(circle, #CCCCCC 1px, transparent 1px);
    background-size: 28px 28px;
    opacity: 0.5;
  }

  .text-signal { color: #FF3B30; }

  /* ── HEADER ── */
  .eco-header {
    max-width: 640px;
    margin: 0 auto 72px auto;
    text-align: center;
  }

  .eco-eyebrow {
    display: flex;
    align-items: center;
    gap: 10px;
    justify-content: center;
    margin-bottom: 16px;
    font-family: 'Roboto Mono', monospace;
    font-size: 11px;
    color: #909090;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .eyebrow-bar {
    width: 2px;
    height: 18px;
    background: #FF3B30;
  }

  .eco-headline {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: clamp(36px, 4vw, 52px);
    color: #0A0A0A;
    line-height: 1.0;
    text-transform: uppercase;
  }

  .eco-subheadline {
    margin-top: 16px;
    font-family: 'Roboto Mono', monospace;
    font-size: 13px;
    color: #909090;
    line-height: 1.7;
  }

  /* ── DIAGRAM ── */
  .diagram-viewport {
    margin-top: 40px;
  }

  .diagram-container {
    position: relative;
    width: 100%;
    max-width: 960px;
    height: 620px;
    margin: 0 auto;
  }

  .connections-svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
    overflow: visible;
  }

  .connector-path {
    stroke: #D8D8D8;
    stroke-width: 1.5;
    fill: none;
    stroke-dasharray: 5 4;
  }

  /* Center Hub */
  .center-hub {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    z-index: 20;
    width: 280px;
    padding: 20px;
    background: transparent;
  }

  .hub-wordmark {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 52px;
    color: #0A0A0A;
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .hub-dot {
    color: #FF3B30;
    font-size: 10px;
    vertical-align: super;
    margin-left: 2px;
  }

  .hub-subtitle {
    margin-top: 8px;
    font-family: 'Roboto Mono', monospace;
    font-size: 11px;
    color: #585858;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .hub-metadata {
    margin-top: 4px;
    font-family: 'Roboto Mono', monospace;
    font-size: 10px;
    color: #AAAAAA;
    letter-spacing: 0.02em;
  }

  /* Stakeholder Cards */
  .stakeholder-card {
    position: absolute;
    background: #FFFFFF;
    border: 1px solid #E8E8E8;
    border-radius: 2px;
    padding: 24px;
    width: 200px;
    z-index: 30;
    transition: border-color 150ms ease;
  }

  .stakeholder-card:hover {
    border-color: #D0D0D0;
  }

  .card-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
  }

  .card-icon {
    color: #0A0A0A;
  }

  .check-badge {
    width: 20px;
    height: 20px;
    border: 1px solid #FF3B30;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 0; /* Keep it square as per reference visual */
  }

  .card-title {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 15px;
    color: #0A0A0A;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .card-desc {
    font-family: 'Roboto Mono', monospace;
    font-size: 11px;
    color: #585858;
    line-height: 1.6;
    margin-bottom: 16px;
  }

  .card-bottom {
    border-top: 1px solid #F0F0F0;
    padding-top: 12px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .card-subdomain {
    font-family: 'Roboto Mono', monospace;
    font-size: 10px;
    color: #AAAAAA;
  }

  .role-tag {
    font-family: 'Roboto Mono', monospace;
    font-size: 9px;
    font-weight: 500;
    color: #FF3B30;
    text-transform: uppercase;
    background: #FFFFFF;
    border: 1px solid #FFE0DE;
    padding: 2px 6px;
  }

  .api-badge {
    font-family: 'Roboto Mono', monospace;
    font-size: 9px;
    color: #AAAAAA;
    border: 1px solid #E8E8E8;
    padding: 2px 6px;
  }

  /* Flow Labels */
  .flow-label {
    position: absolute;
    background: #FFFFFF;
    border: 1px solid #EBEBEB;
    padding: 3px 10px;
    font-family: 'Roboto Mono', monospace;
    font-size: 9px;
    color: #AAAAAA;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    white-space: nowrap;
    z-index: 40;
    pointer-events: none;
  }

  /* ── STATS ── */
  .stat-grid {
    margin-top: 64px;
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0;
    background: #E8E8E8; /* Gap color */
    border: 1px solid #E8E8E8;
  }

  .stat-item {
    background: #FFFFFF;
    padding: 32px;
    text-align: center;
    border: 1px solid #E8E8E8;
    margin: -1px; /* Overlap borders */
  }

  .stat-value {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 48px;
    line-height: 1;
  }

  .stat-label {
    font-family: 'Roboto Mono', monospace;
    font-size: 10px;
    color: #AAAAAA;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-top: 8px;
  }

  /* ── MOBILE ── */
  .mobile-layout {
    margin-top: 48px;
  }

  .mobile-hub {
    background: #0A0A0A;
    border: 1px solid #FF3B30;
    padding: 28px;
    text-align: center;
    border-radius: 2px;
  }

  .mobile-connector-line {
    width: 1px;
    height: 40px;
    border-left: 1px dashed #E0E0E0;
    margin: 0 0 0 40px;
  }

  .mobile-cards-container {
    display: flex;
    flex-direction: column;
    gap: 12px;
    border-left: 1px dashed #E0E0E0;
    margin-left: 40px;
    padding-left: 20px;
  }

  .mobile-card {
    background: #FFFFFF;
    border: 1px solid #E8E8E8;
    border-left: 3px solid #FF3B30; /* Left border accent */
    border-radius: 2px;
    padding: 24px;
    width: 100%;
  }

  @media (max-width: 1023px) {
    .stat-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
