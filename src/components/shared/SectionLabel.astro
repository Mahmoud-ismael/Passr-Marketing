---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className } = Astro.props;
---

<div class:list={['border-l border-metal pl-3 font-mono text-[10px] uppercase tracking-[0.2em] text-metal', className]}>
  {label}
</div>
